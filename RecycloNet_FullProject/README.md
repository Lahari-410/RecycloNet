# RecycloNet ♻️

A real-time recycling pickup tracker using the MERN stack. Users can schedule recyclable item pickups and collectors can accept and complete them.

## Setup
- Install dependencies in /client and /server
- Create `.env` using `.env.example`
- Run backend: node server/index.js
- Run frontend: npm start inside /client
